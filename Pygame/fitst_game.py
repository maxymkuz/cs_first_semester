# import pygame
# pygame.init() #Initialize

# win = pygame.display.set_mode((600, 600))

# pygame.display.set_caption("And that's a game")

# character_width = 40
# character_heigth = 80
# x = 200
# y = 200
# velocity = 10
# run = True

# while run:
#     pygame.time.delay(1000)

#     for event in pygame.event.get():
#         if event.type == pygame.QUIT:
#             run = False

#         keys = pygame.key.get_pressed()

#         if keys[pygame.K_RIGHT]:
#             x += velocity
#         if keys[pygame.K_DOWN]:
#             y += velocity
#         if keys[pygame.K_UP]:
#             y -= velocity
#         if keys[pygame.K_LEFT]:
#             x -= velocity

#         win.fill((0, 0, 0))#color(black)
#         pygame.draw.rect(win, (255, 0,0), (x, y, character_width, character_heigth))
#         pygame.display.update()

# pygame.quit()

elem = False
# elem = 5
if elem:
    print(elem)

